_exclude:
  - tasks.py
  - migrations.py
  - .git

_tasks:
  - "python [[ _copier_conf.template.local_abspath / 'tasks.py' ]] 1"
  - [python, "[[ _copier_conf.template.local_abspath / 'tasks.py' ]]", 2]

_migrations:
  # This migration is never executed because it's the 1st version copied, and
  # migrations are only executed when updating
  - version: v1.0.0
    before:
      - &mig
        - "[[ _copier_conf.template.local_abspath / 'migrations.py' ]]"
        - "[[ _copier_conf|tojson ]]"
    after:
      - *mig
  - version: v2
    before: [*mig]
    after:
      - *mig
      - "rm delete-in-migration-$VERSION_CURRENT.txt"
