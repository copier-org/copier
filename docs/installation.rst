============
Installation
============

At the command line::

    $ easy_install pip
    $ pip install voodoo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv voodoo
    $ pip install voodoo